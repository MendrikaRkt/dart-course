void main(List<String> args) {
  var address = 'First value = Madagascar';
  print(address);

  address = 'Second Value = Antananarivo the capitol';
  print(address); // var can always be reassigned
}