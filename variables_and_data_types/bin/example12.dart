void main(List<String> args) {
    final double myAge = 25; // promoting the variable to a double type to store a decimal value
    print(myAge);

    // Failed to compile
    // final int number = 20.5; denoting the variable as an integer type but storing a decimal value 
}